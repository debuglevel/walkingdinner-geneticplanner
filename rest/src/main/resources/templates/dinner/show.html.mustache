<!doctype html>
<html lang="en">
<head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css"
          integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">

    <title>Dinners</title>
</head>
<body>

<div class="card">
    <div class="card-body">
        <div id="dinners"></div>
    </div>
</div>

<script src="https://code.jquery.com/jquery-3.3.1.min.js"></script>
<script>
(function() {
  var dinnerAPI = "";
  $.getJSON(dinnerAPI)
    .done(function(data) {
        var dinner = data;
        var dinnerDiv = $('<div/>', {
            id: 'dinner',
            class: 'dinner',
            title: 'Dinner '+dinner.id+': '+dinner.name
        }).appendTo('#dinners');

        dinnerDiv.append('<p>'+dinner.id+':</p>');
        dinnerDiv.append('<p>'+dinner.name+':</p>');
    });
})();



</script>

</body>
</html>
